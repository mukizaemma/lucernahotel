; Per-directory PHP limits (used by PHP-FPM/CGI when user_ini is enabled).
; post_max_size must be >= upload_max_filesize and large enough for multiple images + HTML.
upload_max_filesize = 64M
post_max_size = 64M
